using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Windows.Forms;

namespace CSPSS
{
    static class Program
    {/// <summary>
        /// 应用程序的主入口点。
        /// </summary>
        [STAThread]
        static void Main()
        {
            Application.EnableVisualStyles();
            Application.SetCompatibleTextRenderingDefault(false);
            Application.Run(new  LOGIN ());
            //Application.Run(new FINANCIAL_MANAGE.PAYMENT_ORDERT());
            //Application.Run(new FINANCIAL_MANAGE.REQUEST_MONEYT());
            //Application.Run(new FINANCIAL_MANAGE.ADVANCE_PAYMENTT());
            //Application.Run(new STOCK_MANAGE.SCRAPT());
            //Application.Run(new SELL_MANAGE.SELLTABLET());
            //Application.Run(new FINANCIAL_MANAGE.VOUCHER());
            //Application.Run(new FINANCIAL_MANAGE.VOUCHERT ());
            //Application.Run(new BASE_INFO.ACCOUNTANT_COURSE());
            //Application.Run(new FINANCIAL_MANAGE.RECEIVABLEST());
            //Application.Run(new Form1());
            //Application.Run(new BASE_INFO.MOLD_BASET());
            //Application.Run(new STOCK_MANAGE.INVENTORY());
             //Application.Run(new SELL_MANAGE.SELLTABLET());
             //Application.Run(new STOCK_MANAGE.MISC_STORAGET());
            //Application.Run(new STOCK_MANAGE.MISC_STORAGE());
             //Application.Run(new SELL_MANAGE.ORDER());
            //Application.Run(new SELL_MANAGE.ORDERT());
            //Application.Run(new BASE_INFO.MATERIAL ());
            //Application.Run(new BASE_INFO.CUSTOMER_INFOT());
            //Application.Run(new BASE_INFO.EMPLOYEE_INFO());
            //Application.Run(new BASE_INFO.CUSTOMER_INFOT ());
            //Application.Run(new CSPSS.USER_MANAGE .USER_INFO ());
            //Application.Run(new CSPSS.USER_MANAGE.EDIT_RIGHT());
            //Application.Run(new FrmMain());
            //Application.Run(new CSPSS.USER_MANAGE.EDIT_RIGHT ());

        
        }
    }
}
